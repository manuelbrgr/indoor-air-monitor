service: iaq-notification-service

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs14.x

  stage: prod
  region: us-east-1

functions:
  skill:
    handler: index.scheduler
    events:
      - schedule: cron(*/15 * * * *)
